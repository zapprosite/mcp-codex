# MCP Codex CLI - Exemplo de .env (não comitar)
# Preencha com suas chaves e salve como .env

# --- Integrações Externas (segredos) ---
GITHUB_TOKEN=
BRAVE_API_KEY=
FIRECRAWL_API_KEY=
TAVILY_API_KEY=
EXA_API_KEY=
CONTEXT7_API_KEY=
TESTSPRITE_API_KEY=
STRIPE_SECRET_KEY=

# --- Configurações opcionais / clientes locais ---
# Endpoint WS remoto do Playwright (deixe vazio para usar browser local)
PLAYWRIGHT_WS_ENDPOINT=
# Caminho da vault Obsidian (se usar o mcp-obsidian)
OBSIDIAN_VAULT_PATH=

# --- Configurações locais usadas por MCPs de automação/armazenamento ---
SQLITE_DB_PATH=./data/mcp_database.db
FILESYSTEM_BASE_PATH=./
MEMORY_DB_PATH=./data/memory-store/memento.db
# Path do Task Manager (usado pelo @kazuph/mcp-taskmanager)
TASK_MANAGER_FILE_PATH=~/Documents/tasks.json

# --- Opções gerais dos MCPs ---
MCP_LOG_LEVEL=info
MCP_TIMEOUT=30
MCP_MAX_RETRIES=3
